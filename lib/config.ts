// App Configuration
// Edit these values to customize the app for different projects
export const APP_CONFIG = {
  appName: process.env.NEXT_PUBLIC_APP_NAME || "Marmum Admin",
  clientName: process.env.NEXT_PUBLIC_CLIENT_NAME || "Marmum Campaign",
};
